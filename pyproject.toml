[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "precision-ag-system"
version = "0.1.0"
dependencies = [
    "google-genai",
    "lightning",
    "pandas",
    "scikit-learn",
    "python-dotenv",
    "requests"
]